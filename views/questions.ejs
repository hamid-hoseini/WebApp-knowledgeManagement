<!--
<table>
    <tr>

        <th> row</th>
        <th> categories</th>
        <th> description</th>
        <th> company</th>
        <th> mode</th>
    </tr>
    <tr ng-repeat="record in questions">
        <td>{{$index+1}}</td>
        <td>{{record.categories}}</td>
        <td>{{record.description}}</td>
        <td>{{record.name}}</td>
        <td>{{record.mode}}</td>
    </tr>
</table>-->
<div class="quesdiv">
    <div class="mygrid">
        <div id="grid1" class="gridquestions" ui-grid="gridOptions" ui-grid-selection></div>
    </div>
</div>
